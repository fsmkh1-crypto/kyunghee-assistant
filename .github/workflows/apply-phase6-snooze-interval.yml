name: Apply Phase 6 snooze interval

on:
  push:
    paths:
      - 'tools/apply_phase6_snooze_interval.py'
      - '.github/workflows/apply-phase6-snooze-interval.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ui/dark-kyunghee-redesign
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Apply patch
        run: python tools/apply_phase6_snooze_interval.py
      - name: Compile and test
        run: |
          python -m py_compile settings.py timer_engine.py app.py desktop_compact.py
          python -m unittest discover -s tests -v
      - name: Commit implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add settings.py timer_engine.py app.py desktop_compact.py tests/test_settings.py tests/test_timer_engine.py PHASE6_HANDOFF.md
          if git diff --cached --quiet; then
            echo 'No changes to commit.'
          else
            git commit -m 'Add configurable snooze interval'
            git push origin HEAD:ui/dark-kyunghee-redesign
          fi
