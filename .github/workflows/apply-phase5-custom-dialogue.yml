name: Apply Phase 5 custom dialogue

on:
  push:
    paths:
      - '.github/workflows/apply-phase5-custom-dialogue.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ui/dark-kyunghee-redesign
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Apply custom dialogue patch
        run: python tools/apply_phase5_custom_dialogue.py
      - name: Compile and test
        run: |
          python -m py_compile desktop_compact.py messages.py settings.py
          python -m unittest discover -s tests -v
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add desktop_compact.py messages.py settings.py tests/test_messages.py tests/test_settings.py
          if git diff --cached --quiet; then
            echo "No implementation changes"
          else
            git commit -m "Add user custom dialogue"
            git push origin HEAD:ui/dark-kyunghee-redesign
          fi
