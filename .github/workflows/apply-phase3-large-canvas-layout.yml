name: Apply Phase 3 large canvas layout

on:
  push:
    paths:
      - '.github/workflows/apply-phase3-large-canvas-layout.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ui/dark-kyunghee-redesign
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply large canvas layout patch
        run: python tools/apply_phase3_large_canvas_layout.py
      - name: Compile and test
        run: |
          python -m py_compile desktop_compact.py settings.py
          python -m unittest discover -s tests -v
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add desktop_compact.py
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Use large transparent timer canvas"
            git push origin ui/dark-kyunghee-redesign
          fi
